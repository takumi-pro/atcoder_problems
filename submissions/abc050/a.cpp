#include <bits/stdc++.h>
using namespace std;
int main(void) {
    int A, B;
    char OP;
    cin >> A;
    cin >> OP;
    cin >> B;
    if (OP == '+') cout << A + B << endl;
    if (OP == '-') cout << A - B << endl;
}
